import { AuthGuard } from './auth.guard';

describe('AuthGuard', () => {
  let guard: AuthGuard;

  beforeEach(() => {
    
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
